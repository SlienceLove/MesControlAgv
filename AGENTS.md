# 本项目会话与分支规则

本文件适用于仓库及其所有 worktree。子目录规则可以补充要求，但不得放宽这里的分支与协作约束。

## 每次会话开始的必读与检查

1. 在开始修改文件、运行构建或测试、切换分支、提交、合并或推送之前，必须完整阅读 [分支管理文档](docs/BRANCH-MANAGEMENT.md)。不能仅依赖上一会话摘要或记忆。
2. 必须检查当前工作目录、分支、工作区状态及 worktree 占用情况。至少执行：

   ```text
   git rev-parse --show-toplevel
   git branch --show-current
   git status --short
   git worktree list
   ```

3. 向用户简要说明本会话使用的分支、工作目录和任务范围，再开展实现。分析类任务可以只读检查，不因此创建分支或修改仓库。
4. 文档缺失、分支归属不清、工作目录被其他会话使用，或任务会覆盖他人未提交成果时，先完成可行的只读排查，再报告具体问题；不得自行重置、清理或覆盖。
5. 旧分支若缺少本规则或分支文档，不可据此继续开发。先读取当前 `develop` 中的 `AGENTS.md` 和 `docs/BRANCH-MANAGEMENT.md`，按规则迁移任务；仅阅读文件不等于合并或授权变更。

## 分支与并行会话约束

- `master` 是正式基线，不直接开发功能或 UI；`develop` 是唯一集成入口。
- 纯 UI 优化使用 `feature/wpf-ui-layout-optimization`；功能任务从核对后的最新 `develop` 建立明确命名的任务分支。
- 同一任务涉及 UI、接口和业务逻辑时，在同一任务分支完成必要的联动改动，避免接口与页面脱节。
- 并行窗口/会话必须使用不同分支和不同 worktree 工作目录。仅使用不同分支名称不足以隔离工作。
- 一个活跃工作目录只能由一个会话修改。不得替其他会话切换分支、暂存、提交、合并或清理文件。
- 由指定集成会话串行处理合并。顺序固定为：任务/UI 分支 → `develop` 验证 → `master`；验证后的基线再同步回 UI 分支。
- 分支管理文档标记为暂停、历史或归档的分支，不继续累计新功能。未完成任务迁移到最新基线时，必须保留原始提交与未提交成果。
- 提交前检查实际差异，按任务范围暂存。不得整体提交工作目录中的现场日志、数据库、抓包、构建产物或无关修改。
- 合并冲突必须保留双方有效业务行为；不得整边覆盖、删除测试或放宽准入来制造通过结果。合入后执行与影响范围相称的验证。
- 禁止强推公共分支。删除分支、清理 worktree、迁移未提交成果必须先核实准确目标与可恢复性，并在用户授权范围内执行。
- 本规则不构成自动推送、生产部署或现场设备动作的授权。执行这些动作仍须符合当前用户任务范围。

## 规则维护

分支职责或协作流程发生变化时，同时更新 `docs/BRANCH-MANAGEMENT.md`，必要时更新本文件。分支文档中的历史提交号和测试数量是对应检查点的记录，不代表新改动已经验证通过。
