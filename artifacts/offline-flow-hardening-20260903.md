# 标准流程离线加固记录

日期：2026-09-03（Asia/Shanghai）

## 本轮完成

- 自动重定位仅完成接口评估和待现场抓包记录，未发送任何 AGV 重定位写命令。
- WPF 流程管理顶部命令栏改为自适应换行，窄窗口下“发布、试运行、执行模拟”等按钮不再被裁剪。
- AUBO 工作流在进程重启后遇到“控制器已停止但无法证明本次程序完成”时，记录 `Unknown` 并要求人工核销，不再误记成功。
- 物理 Move 批量路径若没有只读物理预检能力则保持 Ready，不创建许可、不派发设备命令。
- 物理预检返回“未通过但没有阻断原因”时增加硬阻断，避免异常驱动绕过门禁。
- WPF 项目增加 `SkipLocalServiceBuild=true` 构建开关，离线编译/打包不会为覆盖现场服务 DLL 而触碰正在运行的现场服务进程。

## 验证

- MES 测试：141 通过，0 失败。
- WPF 测试：363 通过，0 失败。
- MES Release 构建：0 警告，0 错误。
- WPF Release 构建：0 警告，0 错误。
- 最新离线部署目录：`bin/Verify/OfflineRetryDeployFinal4/`；其中 MES/WPF 为本轮新构建，Adapter 沿用已验证的现场版本。

## 现场边界

本轮未恢复网络、未启动新的物理 worker、未申请控制权、未向 AGV/AUBO 发送新写命令。网络恢复后仍须先做新鲜只读预检，再决定是否启用现场批量 worker。

