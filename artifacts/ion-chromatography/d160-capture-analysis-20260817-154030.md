# CIC-D160+ USB capture analysis (2026-08-17 15:40 CST)

## Evidence

- Source archive: `res/d160-capture-result-20260817-154030.zip`
- Archive SHA-256: `94FF4FAAB7511287C0860B6BC5EDE664F7F023228CAF286E1EB0D32BBA2166D5`
- Capture interval: `2026-08-17T07:40:30.7861374Z` to `2026-08-17T07:41:32.9932407Z`
- PCAP: `USBPcap1.pcap`, 726,870 bytes, link type 249 (USBPcap)
- ShineLab process: `ShineControl-Normal`
- D160+ USB path observed as bus 1, device 7
- Host-to-device bulk endpoint: `0x02`
- Device-to-host bulk endpoint: `0x81`
- USB bridge prefixes each received serial chunk with `01 60`; those two bytes are not part of the Modbus RTU frame.

## Frame validation

The capture contains 667 host-to-device Modbus requests:

| Count | Request | Interpretation limited to protocol fields |
|---:|---|---|
| 634 | `01 04 17 70 00 0C F4 60` | Read 12 input registers from `0x1770` |
| 17 | `01 04 17 D4 00 12 35 8B` | Read 18 input registers from `0x17D4` |
| 12 | `01 04 18 38 00 0A F7 60` | Read 10 input registers from `0x1838` |
| 1 | `01 04 19 18 00 18 77 5B` | Read 24 input registers from `0x1918` |
| 1 | `01 04 19 00 00 18 F7 5C` | Read 24 input registers from `0x1900` |
| 1 | `01 04 19 00 00 14 F7 59` | Read 20 input registers from `0x1900` |
| 1 | `01 06 13 E4 5A A5 37 A2` | Write single register `0x13E4 = 0x5AA5` |

After removing the two USB-bridge status bytes from each received chunk and reassembling split serial data, 665 Modbus `0x04` responses pass CRC validation:

| Response data bytes | Valid frames | Unique response bodies |
|---:|---:|---:|
| 20 | 12 | 1 |
| 24 | 633 | 4 |
| 36 | 17 | 2 |
| 40 | 1 | 1 |
| 48 | 2 | 2 |

## Confirmed `0x1900` exchange

- TX at `07:40:59.247423Z`:

  ```text
  01 04 19 00 00 14 F7 59
  ```

- RX at `07:40:59.349349Z` (USB status bytes removed, CRC `0x25CB` valid):

  ```text
  01 04 28 59 41 37 32 36 31 30 37 38 00 00 00 00 00 00 00 00 00 4E 20 00 00 4E 20 00 00 00 00 00 00 00 00 00 00 00 02 00 00 00 00 CB 25
  ```

The response proves that `0x1900` returns 20 input registers. The first bytes contain ASCII-like data (`YA7261078`), but field meanings, units, signedness, scaling, and firmware compatibility are still unconfirmed.

## Candidate read mappings supported by the live UI

The ShineLab values visible during the capture provide stronger evidence for a small subset of the high-frequency reads:

| Register(s) | Observed raw bytes/value | Candidate meaning | Confidence |
|---|---|---|---|
| `0x1770-0x1771` | `5F F1 82 43` -> little-endian float32 `261.885712` | Conductivity (`261.88571 uS/cm` in ShineLab) | High for this software/firmware sample |
| `0x1776-0x1777` | `5F F1 82 43` -> little-endian float32 `261.885712` | Total conductivity (`261.88571 uS/cm` in ShineLab) | High for this software/firmware sample |
| `0x17D8` | `0x0C33` -> decimal `3123` | Column temperature with scale `0.01 C` (`31.23 C` in ShineLab) | High for this software/firmware sample |
| `0x17DB`, `0x17DC` | each `0x012C` -> decimal `300` | Flow-related values with scale `0.001 mL/min` (`0.300 mL/min` in ShineLab) | Medium; setpoint versus measured value is not distinguished |

The two conductivity fields sometimes changed one polling cycle apart, which is consistent with separate current and total/display fields. These mappings remain candidates until vendor documentation or source-level evidence confirms names, scaling, and compatibility across firmware versions.

## Automatically observed write

Immediately after the `0x1900` response, ShineLab sent and the D160+ acknowledged:

- TX at `07:40:59.349455Z`:

  ```text
  01 06 13 E4 5A A5 37 A2
  ```

- RX echo at `07:40:59.365237Z` (USB status bytes removed):

  ```text
  01 06 13 E4 5A A5 37 A2
  ```

This is a Modbus function `0x06` write to register `0x13E4` with value `0x5AA5`. It occurred without a deliberate parameter change during the observation window. It may be a communication handshake or keepalive, but no available evidence establishes its semantic or safety effect.

## Decision

- Passive USB capture and raw request/response reconstruction are successful.
- The known read-only request is confirmed against ShineLab traffic and a valid device response.
- Read register ranges are now evidenced, but their business meanings remain incomplete.
- Do not replay `01 06 13 E4 5A A5 37 A2` or implement any D160+ write path until the vendor or source-level evidence identifies register `0x13E4` and value `0x5AA5`.
- The next implementation may cover a strictly read-only local gateway and raw/status evidence storage. Method loading, injection, start, stop, reset, and all register writes remain disabled.
