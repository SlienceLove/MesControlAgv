# 独立设备观察与地图参照显示

最新部署更新：用户随后回复“继续”，已于2026-09-20 10:00完成MES切换；新MES PID30812，独立设备观察逻辑已上线。下文“尚未切换”为当时交付阶段记录，最新状态以文末为准。

## 当前交付状态

用户批准：设备不必全部同时开机，开盖／分液离线不应拖住AGV状态和三维活动；截图辅助元素仅在开启地图参照时显示，设备模型／绿色状态标签保留，去掉正上方俯视入口。

代码、离线验证与独立审查完成。新只读预览已切换；后台修复已发布成独立MES包，但运行中的MES尚未重启，所以后台隔离逻辑尚未在现场服务生效。没有发送任何设备动作，没有提交或合并，也未覆盖旧运行包。

## 后台变更

- `PhysicalReadinessSupervisor` 从整轮串行等待改为按设备独立后台循环；正常设备立即发布自己的结果，刷新下一帧不等待离线工作站。
- 每设备独立DI作用域、单在途门闩。手动全量刷新与后台循环共享该设备门闩，忙时不排队叠加请求；强制完整预检请求保留到下一次可用探测。
- `PhysicalReadinessSupervisor:ProbeTimeout` 默认8秒，有界等待。超时只更新该设备失败状态；忽略取消的底层请求继续占用自己的作用域和门闩直至结束，其迟到成功结果不会用于恢复Ready，迟到异常统一观察。
- 聚合RefreshInProgress改为并发计数。AGV自身数据过期、稳定窗口、epoch、重连授权、控制归属及完整流程所需设备门槛不变。没有把工作站假装在线，也没有通过增大新鲜度窗口绕过检查。
- 原三维WPF三个设备状态循环及独立位姿源已独立，无需重写；增加原生测试证明工作站不响应时AGV与机械臂各自更新，AGV动画不中断。

## 界面变更

地图参照默认关闭，以下内容统一受开关控制：右侧小地图、黑色地图轮廓／扫描点、蓝色路线、LM站点圈和标签、三维左上来源信息及左下示意／操作提示。路线开关不能绕过地图参照主开关，再次打开会保留路线显示偏好。

地面和1m网格、固定设备、AGV整机与实时移动、设备状态标签始终保留；离线设备仍显示真实异常颜色和文本。WPF状态卡和位置异常提示不隐藏。移除正上方俯视按钮与公开top命令，保留摆正全景、设备区、旋转缩放、聚焦和地面标定选点。

原GLB、坐标变换、自适应动画及清晰度未改。

## 验证

- MES就绪相关27项通过，其中6项新增隔离测试覆盖长期无响应、并发手动刷新、迟到成功／故障、停止、重连再授权、取消边界故障。
- 完整MES535/535通过，WPF533/533通过，JS20/20通过。
- 原生WPF/WebView离线冒烟通过，证据 `artifacts/digital-twin-independent-devices/smoke-r2/result.json`。工作站模拟永不返回且只发起一次读取，AGV状态与连续移动、机械臂状态均保持刷新；地图参照开关不影响位置跟随。0.3m/s抖动转向测试停顿帧约1.68%，帧间隔中位数16.7ms，准确停稳；不是新的实机运动证明。
- 截图同目录 `reference-off.png`／`reference-on.png`，均为离线测试。
- 首轮原生旧的D160点击用例因透视中遮挡失败，改为对可见AGV网格实际射线点击，聚焦、坐标不变和标定拾取复测通过。
- 首次完整MES回归中，压缩到100/250ms的时序用例间歇失败，单独与完整复跑通过；该组时序测试已设为不与其他ASP.NET启动测试并行，最终完整回归通过。生产8秒超时／10秒新鲜度没有因测试修改。
- 独立审查指出超时与异常完成竞态，已修复为观察所有完成/未完成底层任务并补充边界测试；增量审查无剩余问题。

## 发布与部署边界

- 新预览：`artifacts/digital-twin-independent-devices/preview/CalibrationPreview.exe`。
- 新MES：`artifacts/digital-twin-independent-devices/mes/`。
- 原只读预览PID26812已核对“尚未选点”后正常关闭，新预览PID11040，证据目录 `artifacts/digital-twin-independent-devices/live-session-20260920/`。
- 原运行MES15445/PID24844、机器人控制Adapter15441/PID33204、工作站Adapter15041/PID15244、只读Adapter5041/PID29128均未重启。PID是记录值，后续操作须重新核对。

下一步需获准切换运行MES，才可验证真实工作站断开时的后台独立刷新。切换前检查无活动流程/导航/设备任务，保存当前启动参数和数据库一致性备份；使用原数据库与现场配置，不启动第二个调度器，不覆盖旧包。新supervisor实例和设备epoch要求重新确认许可，不复用旧许可或恢复旧任务。仅切换软件不包含导航、初始化工作站或关闭现场工作站的授权。

## MES切换完成（2026-09-20 10:00–10:02）

- 用户批准仅切换MES，不执行设备动作。切换前确认AGV在LM1无任务、控制权none；ARM程序Stopped；工作站Idle，TEST-001为历史Completed；排程、租约和所有执行记录均无活动项。
- 使用SQLite在线backup API保存一致性快照（包含已提交WAL内容），不是只复制运行中的mes.db。备份和配置、旧启动命令及新日志位于 `artifacts/digital-twin-independent-devices/cutover-20260920-0958/`。备份 `mes-before.db`，540672字节，quick_check=ok。
- 核对原端口及命令行后停止旧MES PID24844，用新DLL启动PID30812，仍监听127.0.0.1:15445，使用原contentRoot与原mes.db。新DLL SHA256=90729341BC2DE9F093C4F986D795B390934ADF8AD5BE090577C4E5764EA2D942。旧程序目录未覆盖。
- 原三个Adapter PID33204／15244／29128和三维预览PID11040不变。新MES health=ok，三个设备均Ready、各自观测时间持续更新；三维窗口自动恢复状态读取。
- 新supervisor=676bc19cb67647198315bf0a8353995f，设备epoch从1开始并要求新许可，不复用旧supervisor许可；未自动授权、导航、运行机械臂或工作站。
- 切换后逐表比较与备份中的所有原始记录完全一致，quick_check=ok；12条导航、8条设备操作、1条已完成流程不变；现场配置SHA256未变。新日志未发现HTTP POST/PUT/DELETE或厂家StartExperiment/Init出站调用。
- 最终AGV仍LM1、无任务、controlOwner=none，排程和活动租约为空。证据 `verification-summary.json`、`verification.json`、`session.json`。
- 本轮没有人为关闭现场工作站来制造掉线。设备隔离故障场景已通过离线测试；现场掉线场景如需追加，可在用户安排设备服务关闭时只读核对，不自动关停现场设备。
