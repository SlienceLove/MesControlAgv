# 敖博机械臂与视觉模块对接检查清单

## 当前状态

- ✅ 机械臂与视觉模块已通过 TCP/IP 通信
- ✅ 完成基础标定流程（设置了 Mark 点）
- ⏳ 待确认：中控系统如何对接

## 需要厂商提供的技术资料

### 1. 敖博机械臂通信协议

#### 1.1 连接信息
- [ ] IP 地址和端口号（例如：`192.168.1.100:8080`）
- [ ] 是否需要登录认证
- [ ] 连接超时时间建议
- [ ] 是否支持多客户端同时连接

#### 1.2 通信协议格式
- [ ] 报文格式（JSON / XML / 二进制 / 自定义协议）
- [ ] 帧头/帧尾标识
- [ ] 长度字段定义
- [ ] 校验方式（CRC / MD5 / 无）
- [ ] 字节序（大端 / 小端）

#### 1.3 命令列表
请提供以下命令的具体格式和示例：

```
命令类型              | 命令代码 | 请求格式 | 响应格式 | 备注
---------------------|---------|---------|---------|-----
连接/登录            | ?       | ?       | ?       |
获取状态             | ?       | ?       | ?       | 位置/速度/力/错误
使能/失能            | ?       | ?       | ?       |
回原点（Home）       | ?       | ?       | ?       |
移动到指定位置       | ?       | ?       | ?       | 关节空间/笛卡尔空间
抓取（夹爪闭合）     | ?       | ?       | ?       | 力控参数
放置（夹爪松开）     | ?       | ?       | ?       |
暂停                 | ?       | ?       | ?       |
继续                 | ?       | ?       | ?       |
急停                 | ?       | ?       | ?       |
清除错误             | ?       | ?       | ?       |
```

#### 1.4 坐标系定义
- [ ] 机械臂基坐标系原点位置（相对于 AGV 小车）
- [ ] 坐标单位（毫米 / 米）
- [ ] 角度单位（度 / 弧度）
- [ ] 坐标系方向（右手系 / 左手系）
- [ ] 工具坐标系（TCP）定义

#### 1.5 状态码
- [ ] 机械臂状态定义（空闲/运行/暂停/错误/急停）
- [ ] 错误码列表及含义
- [ ] 警告码列表

#### 1.6 性能参数
- [ ] 最大负载（克）
- [ ] 最大工作半径（毫米）
- [ ] 运动速度范围（毫米/秒）
- [ ] 定位精度（毫米）
- [ ] 重复定位精度（毫米）
- [ ] 命令响应时间（毫秒）

### 2. VisionGroup2 视觉模块接口

#### 2.1 连接信息
- [ ] IP 地址和端口号（例如：`192.168.1.101:8081`）
- [ ] 通信协议（HTTP REST / TCP Socket / gRPC）
- [ ] 是否需要认证

#### 2.2 API 接口列表

**基础操作**
- [ ] 连接/登录接口
- [ ] 获取系统状态
- [ ] 获取相机参数

**拍照与识别**
- [ ] 触发拍照接口
  - 参数：曝光时间、增益、ROI 区域
  - 返回：图像路径或 Base64
- [ ] 识别物体接口
  - 参数：图像 ID、识别类型（条码/颜色/形状）
  - 返回：识别结果、置信度、边界框
- [ ] 定位物体接口
  - 参数：识别结果 ID
  - 返回：物体中心坐标（X, Y, Z）、旋转角度

**标定相关**
- [ ] 获取标定参数接口
- [ ] 更新标定参数接口（如果支持）
- [ ] 坐标转换接口（相机坐标 → 机械臂坐标）

#### 2.3 坐标系信息
- [ ] 相机坐标系定义
- [ ] 相机安装位置（固定安装 / 机械臂末端）
- [ ] 标定后的转换矩阵格式
- [ ] 坐标转换是否由视觉系统完成，还是需要中控计算

#### 2.4 Mark 点信息
- [ ] Mark 点数量和位置
- [ ] Mark 点的作用（定位基准 / 姿态矫正）
- [ ] 是否每次都需要扫描 Mark 点

### 3. 机械臂与视觉联动

#### 3.1 当前联动方式
- [ ] 视觉系统如何触发机械臂
  - 方式 1：视觉直接控制机械臂（不推荐）
  - 方式 2：视觉提供坐标，机械臂接收指令
  - 方式 3：中控协调（推荐）
  
#### 3.2 标定流程
- [ ] 标定步骤文档或视频
- [ ] 标定精度验证方法
- [ ] 标定有效期（是否需要定期重新标定）

#### 3.3 实际工作流程
请确认当前厂商调试时的流程：

```
步骤 1: [AGV 到达指定位置]
步骤 2: [视觉触发拍照] - 谁发起？如何触发？
步骤 3: [视觉识别物体] - 返回什么数据？
步骤 4: [坐标转换] - 谁负责转换？
步骤 5: [机械臂接收坐标] - 接收什么格式的数据？
步骤 6: [机械臂移动抓取] - 完成后如何反馈状态？
```

### 4. 网络拓扑

请确认实际的网络连接方式：

```
选项 A: 中控直连所有设备
┌─────────┐
│ 中控PC  │
└────┬────┘
     │
     ├──→ [AGV 控制器]
     ├──→ [机械臂控制器]
     └──→ [视觉工控机]

选项 B: AGV 小车内部网络
┌─────────┐         ┌──────────┐
│ 中控PC  │────────→│ AGV 车载 │
└─────────┘         │  交换机  │
                    └────┬─────┘
                         ├──→ [机械臂]
                         └──→ [视觉]

选项 C: 其他拓扑
[请说明]
```

当前使用的是：**选项 ____**

- [ ] 各设备的 IP 地址分配表
- [ ] 网络延迟情况（ping 值）
- [ ] 是否有防火墙或网络隔离

### 5. 安全机制

#### 5.1 急停联动
- [ ] 机械臂如何接收急停信号
- [ ] AGV 急停时，机械臂是否同步停止
- [ ] 急停后的恢复流程

#### 5.2 碰撞保护
- [ ] 机械臂是否有力矩检测
- [ ] 碰撞后的处理机制
- [ ] 工作空间限制设置

#### 5.3 异常处理
- [ ] 视觉识别失败时的处理
- [ ] 机械臂运动异常时的处理
- [ ] 通信超时的处理

### 6. 示例数据

请提供实际测试时的完整数据样例：

#### 6.1 机械臂命令示例

**请求示例（移动到指定位置）**
```json
{
  "command": "move_to",
  "target_pose": {
    "x": 300.5,
    "y": 150.2,
    "z": 100.0,
    "rx": 0.0,
    "ry": 0.0,
    "rz": 90.0
  },
  "speed": 50
}
```

**响应示例**
```json
{
  "ret_code": 0,
  "ret_msg": "success",
  "state": "moving"
}
```

#### 6.2 视觉识别示例

**请求示例（识别物体）**
```json
{
  "capture_id": "12345",
  "target_type": "shape"
}
```

**响应示例**
```json
{
  "found": true,
  "object_type": "cylinder",
  "position": {
    "x": 305.2,
    "y": 148.7,
    "z": 5.0
  },
  "rotation": 15.3,
  "confidence": 0.95
}
```

## 中控系统对接方案

基于获取到的信息，我们将：

### 阶段 1: 协议适配层开发（1 周）
- [ ] 实现 `AobotRobotArmClient`（TCP 通信）
- [ ] 实现 `VisionGroup2Client`（HTTP/TCP 通信）
- [ ] 编写协议解析和序列化代码

### 阶段 2: 驱动层封装（1 周）
- [ ] 实现 `IRobotArmDriver` 接口
- [ ] 实现 `IVisionDriver` 接口
- [ ] 统一异常处理和日志记录

### 阶段 3: MES 编排集成（1 周）
- [ ] 实现 `CompoundTaskService`
- [ ] 设计综合任务状态机
- [ ] 实现错误恢复机制

### 阶段 4: 测试验证（1-2 周）
- [ ] 单设备独立测试
- [ ] 多设备联动测试
- [ ] 完整流程验收

## 联系方式

**厂商技术支持**
- 敖博机械臂：[联系人/电话]
- VisionGroup2：[联系人/电话]

**预约现场调试**
- 日期：________
- 地点：________
- 参与人员：________

## 附录：测试场景

### 场景 1: 简单搬运（无视觉）
1. AGV 导航到 A 点
2. 机械臂在固定坐标抓取物体
3. AGV 导航到 B 点
4. 机械臂在固定坐标放置物体

### 场景 2: 视觉引导抓取
1. AGV 导航到 A 点
2. 视觉拍照识别物体位置
3. 机械臂根据视觉坐标抓取
4. AGV 导航到 B 点
5. 机械臂放置物体

### 场景 3: 异常恢复
1. 模拟视觉识别失败
2. 模拟机械臂抓取失败
3. 模拟通信超时
4. 验证恢复流程

---

**填写说明**：
- [ ] 待确认项
- ✅ 已确认项
- ⏳ 进行中
- ❌ 不支持/不适用
