# UI 导航简化验收记录

分支：`feature/wpf-ui-layout-optimization`。

- 隐藏地图视图导航入口及旧地图标签页，保留数字孪生入口。
- 隐藏就绪／地图／审计导航入口；原标签页继续保持隐藏，底层准入和审计能力不变。
- 物料管理保持独立一级导航组，包含“物料与库存”和“样品管理”；样品不出现在运营总览中。

验证：12 项 MainWindowNavigationTests 通过；Release 发布成功。导航测试覆盖隐藏入口、数字孪生可见性、物料分组及子菜单切换。

用户授权关闭旧 UI 后，已通过客户端专用启动脚本启动新现场窗口，使用原外部 MES/Adapter、现场配置和流程库，保持 Physical 安全模式。本次未重启后台服务或执行设备动作。窗口自动化检查确认新菜单可见，流程库哈希与重启前一致。

本地发布目录：`artifacts/ui-navigation-20260920/`。现场配置启动脚本：该目录下 `Start-FieldUi.ps1`。这些是本机产物，不作为源码提交。

本轮确认后仅提交并推送 UI 分支；由指定集成会话按项目规则合入 develop，再更新 master。
