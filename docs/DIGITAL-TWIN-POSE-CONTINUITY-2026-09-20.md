# 数字孪生动画衔接优化（已完成一圈实机复验）

最新结论：2026-09-20 09:18–09:20 已在用户重新确认现场准备好后完成一圈实机复验，用户观察首段后反馈“明显顺畅，效果可以”。三段全部 arrived、控制权已释放。下文离线阶段“不启动实机／未替换预览”保留为阶段记录，最新状态以文末为准。

## 范围和现状

用户在一圈 LM1→LM7→LM6→LM1 后确认路线、车头方向一致，但反馈画面不连贯。已批准持续优化至需要实机验证。本阶段未发送设备指令，未开启新一圈，未重启 MES/Adapter，未修改地图对齐和原 GLB，也未提交或合并。

## 修改

原先坐标读取后等待 500 ms，浏览器却在 200 ms 内完成动画，剩余时间等待新数据。改为独立 `PoseMotion` 状态机：

- 以浏览器单调接收时间估计正常帧间隔，新间隔权重 0.25；插值时长为估计值×1.1，限制 450–1000 ms，初始 550 ms。
- 新坐标先计算旧动画在当下的位置，再连续衔接；航向仍采用最短角差，不外推、不按规划路线生成运动。
- 相同目标不会不断重启动画，确保停车后准确收敛；静止时不持续请求动画帧。
- 有效期不因动画衔接延长，重复帧也不能延长旧段有效期；过期冻结在最后呈现位置。stop/reset、隐藏与卸载清除在途状态。
- 首次有效定位仍直接落点；清晰度、几何面数、像素比、原500 ms轮询频率、跳变／置信度守卫均保留。

## 离线验证

确定性 JS 测试使用同一时间序列比较新旧算法（不是 GPU 掉帧率）：

| 输入 | 旧200ms方案等待占比 | 新方案等待占比 |
| --- | ---: | ---: |
| 固定500ms间隔 | 59.95% | 0% |
| 450–650ms抖动 | 63.30% | 3.03% |

JS 共19项通过，覆盖续接、最短角差、重复目标、过期、停止、初次定位及相机/地图回归。WPF533项回归通过。

原生离线宿主使用现有 WPF 500 ms查询→消息桥→WebView渲染链路，模型1287310面及像素比未降低。测试包含0.15m/s匀速，以及0.3m/s加0/120/40/80/0/60ms通信延迟并缓慢转向。运动统计只分析1500–4500ms区间，停车段要求最后四帧位置和最短角差误差小于1e-6；截图与合成坐标均标为离线数据。

第二轮原生测量：匀速停顿帧0%，抖动转向约1.12%；采样帧间隔中位数16.7ms，P95约17ms。该结果来自本机离线窗口，不是现场网络或显示器呈现帧率保证。

最终7秒窗口复测通过：匀速停顿帧0%，抖动转向约1.68%，两个场景各分析180个运动采样帧；中位数16.7ms、P95为17ms、最大17.9ms，均无倒退并到达准确终点。全套原生姿态、过期/低置信度冻结、地面选点和卸载回归通过。Release预览发布成功，已核对包内 viewer.js、pose-motion.mjs 与源文件哈希一致，GLB仍为96CA7F34C189C9C1ADD62C29441A7A9B16E67619C96659C259DCA7EA8412671A。

独立审查未发现运行逻辑问题；依反馈补充终点精确断言，并将采集窗口从6.2s增至7s，覆盖末帧接收延迟及最长1s收敛，避免测试时间边界误报。

## 包和下一步

新包：`artifacts/digital-twin-pose-continuity/package`；旧包 `artifacts/digital-twin-view-alignment/package` 保留。原生证据目录 `artifacts/digital-twin-pose-continuity/smoke-final`。

旧的现场只读预览尚未自动替换，避免将已有窗口误认为新版。需要实机复验时，核对无选点草稿后关闭旧预览，再启动新包：

```powershell
CalibrationPreview.exe --live-readonly <新的证据目录> --schematic-follow
```

下一步需要用户确认现场条件后，切换新预览并跑一圈既定路线，对比直行／转弯／停车的连续感以及可接受的显示延迟。本次不自动进行。若仍感到卡顿，再根据实际帧间隔和数据间隔区分渲染负载与通信问题，不先降低模型精度。

## 2026-09-20 现场复验完成

- 用户明确“准备好了，确认重新执行一遍”；admin 操作／监护，范围仅 AGV，一圈 LM1→LM7→LM6→LM1，原配置速度上限0.3m/s。未发机械臂或工作站动作。
- 预检 AGV Ready、LM1、无任务、controlOwner=none，工作站通信正常。没有复用上一圈任务，三段各创建新许可并下发一次。
- 确认旧预览无选点草稿后正常关闭 PID37032，启动新版 PID26812；启动证据 artifacts/digital-twin-pose-continuity/live-session-20260920-0915/startup.json 显示场景正常、跟随开启、位姿校验通过。新版 pose-motion.mjs SHA256=87AAF3D0D3510ABCC0860DD5AE41396959EFB791F5E36C192A5D1EC20B491BAA。
- 三段验收ID：54e674e8-d84f-4236-bac8-0a5253102f28、cccf1aed-91eb-4aa8-88bb-3dff4a75b24e、e2b328e8-e747-4e87-a1b3-f1998b963175，全部 arrived，无 LastError。
- 首段后用户评价“明显顺畅，效果可以”；其后两段完成、采样中三维坐标/航向持续更新，未观测到同步暂停。不把用户首段反馈扩大为逐帧量化性能或长期多轮稳定性验收。
- 收尾控制权释放审计3797975f-d69c-48f4-ae66-fb6cba379dbe，Succeeded。最终LM1、currentTaskId=null、controlOwner=none。
- 证据根目录 artifacts/physical-acceptance/agv-twin-continuity-20260920-0915，summary.json包含逐段结果与最终回读。保留新版预览供查看，不再自动加跑；未提交或合并。
