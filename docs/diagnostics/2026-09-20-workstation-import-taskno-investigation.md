# 2026-09-20 导入失败的本地兼容排查

## 新发现

不能把本轮问题直接定性为纯厂家故障。中控正式任务号生成器当前输出42字符，而厂家既有数据库脚本定义任务号为 `varchar(20)`，存在明确的历史版本兼容差异。

- 冻结准备记录：`artifacts/workstation-field-20260920-0935/prepared.json`。
- 实际提交任务号：`MES-20260920014125514-84cac287ab024f2ebd33`，**42字符**。
- 任务名称 `test`，4字符；文件名加 `.xlsx` 为47字符。
- 中控生成点：`src/MesControlAgv.Mes/Services/ExperimentWorkstationPreparationService.cs` 的 `CreateVendorTaskNo`，截取长度42。
- 厂家旧脚本：`D:/Project/Github/Mes/.tmp_modeldll_inspect/MODELDLL/system.sql`，第679–685行定义 `Tab_TaskInformation.TaskNo varchar(20) UNIQUE not null`。
- 脚本文件时间2026-06-29；SHA256：`FC6D1DAEA34F6A43EF5E7A621D81224826E6C573E9659E7046E5CC896A6F1E79`。
- **没有读取现场数据库结构，不能断言其仍是20字符，不能断言这就是本次失败的唯一根因。** 严格SQL模式下超长值可能被拒绝，其他设置下可能被截断；需要厂家日志/当前表结构验证。
- 两个中控测试来源ID各20字符，本轮尚未发送条码更新，不能将其视为已验证的厂家字段兼容。

## 其他厂家侧核对点（旧代码证据）

既有反编译 `WcfServiceDLH.Service.decompiled.cs` 位于 `artifacts/workstation-v102-template-20260918/vendor-reference/`：

1. `GetExperimentalTaskTemplate` 查不到任务时回退读取旧任务。本次回读旧号现象与此一致，不是新任务被自动重编号的证据。
2. `Import_ExperimentalTaskExcel` 多段复用成功标志。任务主表失败后，后面的轨迹处理可能覆盖最终结果，所以“导入成功”不足以证明任务主表和所有轨迹都完整。
3. 旧脚本的 `LayoutName/WorkflowName` 为NOT NULL，旧任务插入代码未提供它们；现场是否已有默认值/放宽约束须确认，不能自行编造布局和工作流来填充。
4. 旧数据库为MySQL；旧插入语句使用 `INSERT ... VALUE(...)` 本身不能按SQLite语法认定为错误。
5. 旧双来源分组条件使用多个字段的AND比较，存在来源混合风险；本次新任务尚不可查询，无法验证当前厂家版本是否仍有该问题。保留中控逐行回读，不放宽匹配。

## 最小修正建议与后续落实

用户已确认并完成本地修正：正式新号采用WS+18位随机大写十六进制，共20字符；历史记录不动。代码 `0f31868`，测试和原上传复现包见[短号兼容验证](2026-09-20-workstation-short-taskno-validation.md)。下文保留最初的范围约定。

新创建的厂家任务号限制在20字符以内，保持独立随机性；不简单截掉现有长号的尾部，以免失去区分不同任务的部分。中控内部Job/Preparation等GUID和关联关系保留，不要求从厂家任务号解读完整时间信息。仅约束**新号生成**，不修改既有任务的读取/持久化值。

补充长度、字符集、多次生成不重复、幂等准备及完整业务链的离线测试。不要重写本轮Unknown记录，不自动重新导入，不替换现场服务或启动设备。该兼容修正只能消除一个已知历史约束风险，不能替代真实导入验收。

已向用户提供“先做本地最小修正”与“先等厂家确认”的选择；用户确认前者后才实施。代码已修复并离线验证，尚未部署现场或重新导入。

## 可转发厂家的补充文字

> 我们本地继续查到一个可能原因：中控这次任务号长度是42位，您之前给的数据库脚本里TaskNo是varchar(20)。麻烦确认现场现在是不是仍限制20位，并看9:42左右日志是否有字段超长或必填字段缺默认值的错误。如果任务主表写入失败，导入接口也请返回失败原因；查不到指定任务时不要返回旧任务模板。我们这边准备把新生成任务号缩短到20位以内，先做本地兼容验证。

## 本轮边界

初次排查只做诊断；后续经用户确认修改了新号生成代码并离线验证，但仍没有新发厂家请求、没有重新导入或启动、没有改现场数据库或运行服务配置，也没有合并日常分支。42位原请求和Unknown记录继续原样保留；实际上传XLSX已按原hash还原并打包，没有用LOCAL预览替代。
