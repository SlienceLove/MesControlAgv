# 2026-09-20 两来源 16 孔 50 µL 真实导入验收（未通过）

## 结论

用户确认现场准备好，实际体积按任务设置执行，并选择由中控生成两个测试来源 ID。本轮走正式中控任务准备/导入路径，不使用 LOCAL 预览冒充正式任务。导入后回读未通过，**没有运行准入或启动设备**，不能计为完整真机验收或进行日常分支合并。

## 本轮身份

- 源码：`09190d0`（归档文档头 `0a4bf79`），使用已通过 Release 构建的隔离输出。
- 批次：`FIELD-20260920-0935`。
- 1 号大瓶 `CYC-001-1000/(1,1)` → `SRC-20260920-0935-01` → `FYB-001` 8 孔。
- 2 号大瓶 `CYC-002-1000/(1,1)` → `SRC-20260920-0935-02` → `FYB-002` 8 孔。
- 来源 ID 是用户同意分配的本轮测试标识，不是扫码读取结果。
- 各组目标顺序：`(1,1),(1,2),(1,3),(1,4),(2,1),(2,2),(2,3),(2,4)`；每孔 50 µL，各来源 400 µL，总计 800 µL。
- Workflow：`24850369-a8bd-4eb0-94ac-c575db3073bd` v1。
- Plan：`3ad00926-b17f-4de2-bb59-18aba8a536fe` v1。
- Job：`64db094f-f9b8-4671-b7c4-5e82a98362fa`。
- Schedule：`1c460c9c-a041-46ee-802d-fcb578a81fbf`。
- Preparation：`21da2938-ac94-4dbe-a06b-6ba6fc37ade1` rev1；当前 `Unknown`，不是 Imported。
- 新任务号：`MES-20260920014125514-84cac287ab024f2ebd33`。
- 发送文件名：`MES-20260920014125514-84cac287ab024f2ebd33.xlsx`。
- 生成文件 SHA256：`7B51C8ED1CC6549E14AE82133951BDC1F77093502C10F78CE018BFA0888808EA`。
- Job 保持 Scheduled，`workflowRunId=null`；没有真实运行记录。

## 实测时间与现象（北京时间）

1. 09:35 前置只读：设备 Idle/0、错误码0，原 `test` 为 Waiting。
2. 09:41 正式准备成功：来源已绑定，保存16条50µL转移；枪头 `QT-001/(0,0)` 原样保留。
3. 09:42:06 发出唯一一次 `POST ImportExperimentalTask`，返回 HTTP 200 且业务“导入成功”（正式驱动只有识别该成功响应才进入回读）。
4. 09:42:10 按新任务号回读，Excel 内任务号却是 `202654250001`，中控标记 Unknown，未继续双条码写入或启动。
5. 只读任务列表没有新任务；返回的 `202654250001` 是 2026-08-25 13:54:49 的旧任务，而非本轮新编号。不能把此现象解释为厂家自动改号。
6. 直接 UTF-8 读取厂家 `GetTaskDetails?TaskNo=<新号>` 与 `GetTrajectoryParameterDetails?TrajectoryTaskNo=<新号>`，均为 HTTP 200 / `Code=201, Data=无数据`。
7. 原 `test` 的任务号、名称和全部转移字段与本轮开始前一致（仍16条、每孔1000µL），未覆盖原模板。
8. 本专用 Adapter 出站日志：导入1次，启动0次，条码更新0次，初始化0次。设备仍 Idle/0、错误码0。

## 已核对与尚不确定

- 中控上传帧格式与既有厂家测试客户端一致：文件名Base64长度的单字节Base64前缀 + 文件名Base64 + XLSX Base64，原始 body，Content-Type application/json。
- 采用同一生成器的 LOCAL 预览用本机 Excel COM 只读打开成功；工作表“实验任务表”为2行4列，“移液参数表”为17行11列，K2为50。只证明生成格式可被本机 Excel 读取，不证明当前厂家落库行为或上传文件在其电脑上的完整性。
- 既有反编译服务代码中，模板查询不存在的任务时会回退到旧任务；与本次实测一致。旧导入实现逐段修改同一成功标志，存在前段失败被后段成功覆盖的可能；旧新增任务 SQL 也可供厂家排查，但本轮未拿到当前运行服务的日志/实现，不能直接认定具体失败行。
- 因此已确认“接口成功应答与可查询结果不一致”，具体是当前厂家 Excel 解析、字段约束、数据库写入或部署版本问题，需要其服务日志定位。不能排除部分写入/孤立轨迹，不自动重发或人工改数据库放行。
- 本轮没有验证实际移液动作、双条码同步、Running/Completed 闭环或 WPF 实时显示。

## 本地服务与证据

专用验收目录：`artifacts/workstation-field-20260920-0935/`（运行数据 ignored，保留本机）。

- 新 Adapter：`http://127.0.0.1:15042`，本轮启动 PID 24616。
- 新 MES：`http://127.0.0.1:15045`，本轮启动 PID 13396。
- 两者从 `C:/Users/33206/AppData/Local/Temp/mes-workstation-layout-20260920-build/bin/` 的对应 release 程序启动；Production 环境，专用 contentRoot/config/SQLite。
- MES 只启用工作站 worker；未准入任何运行。原15041旧 Adapter、数字孪生和三设备联调进程未停止、未改配置。未启动或切换 WPF。
- PID 仅为当次记录，后续操作前重新核对进程路径/端口。
- `preflight-template.json`、`central-setup.json`、`prepared.json`、`import-result.json`、`import-readonly-reconciliation.json`、`final-readonly-checks.json`、`vendor-query-utf8.json` 及 adapter/mes 日志/数据库保存本轮证据。PowerShell默认解码产生的乱码字段，以后者显式UTF-8记录为准。
- 本地调试请求曾因枚举/日期JSON序列化返回400，修正后复用了已有唯一Job；这些是中控创建/排程请求，未产生额外厂家写命令。

## 下一步 / 可转发厂家

后续本地排查发现：中控生成号42字符，厂家旧数据库脚本的TaskNo限制20字符；这是待核实的兼容原因，不应把问题直接定性为纯厂家故障。字段长度、旧代码证据和最小改动建议见 [任务号兼容排查](2026-09-20-workstation-import-taskno-investigation.md)。现场表结构仍待厂家确认，本轮Unknown记录不变。

> 今天9:42左右试了一次新任务导入，两来源、16孔、每孔50µL。ImportExperimentalTask 返回“导入成功”，但按新任务号 MES-20260920014125514-84cac287ab024f2ebd33 查询任务和轨迹都返回201“无数据”；下载该任务模板反而返回8月25日旧任务202654250001。中控目前没有发启动。麻烦帮忙查一下本次导入的Excel解析/入库日志，以及服务目录Excel下收到的 MES-20260920014125514-84cac287ab024f2ebd33.xlsx，确认新任务没生成的原因。另外查不到指定任务时，希望返回无数据，不要回传其他任务模板。

拿到厂家日志/修复后，先只读对账本轮 Unknown 记录与厂家任务，不重复上传本轮请求或直接启动旧任务。再按明确的处理方案安排新一轮真实验收；完整闭环通过前不合入 `feature/wpf-ui-layout-optimization`。
