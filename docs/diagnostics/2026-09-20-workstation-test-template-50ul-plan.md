# `test` 双来源模板核对与 50 µL 实验约定（2026-09-20）

## 用户确认

用户以厂家界面截图明确：`CYC-001-1000` 是 1 号大瓶，`CYC-002-1000` 是 2 号大瓶。随后确认先整理当前模板，实际实验时按每孔 **50 µL** 创建一个新的任务模板。

因此后续以小写 `test` 为来源模板，不继续用旧 `TEST-001` 的单来源示例；保留原 `test` 不变，不覆盖、不直接启动原任务。后续已按本约定完成零坐标最小兼容并生成**本地预览 XLSX**，见[实现与验证记录](2026-09-20-workstation-zero-tip-preview-validation.md)；没有创建正式准备记录或向厂家创建/导入任务。

## 本次只读核对结果

- 设备状态：读取时 `Idle / 0`；任务 `test` 为 `Waiting / 等待运行`。这些是当时观察，不保证以后设备仍空闲。
- 下载入口：`GET /api/workstations/SAMPLE-WORKSTATION-01/protocol/ExperimentalTaskTemplate?key=test`。
- 返回 `Code=200`、文件名 `实验任务模板_test[test].xlsx`；Excel 内任务编号和名称均为 `test`，并非请求不存在任务后返回其他模板。
- 接口任务列表当时同时包含旧 `TEST-001`（Completed）和 `test`（Waiting）；本轮以用户正在查看的 `test` 为准。
- 两张工作表仍为“实验任务表”“移液参数表”，后者 11 列，共 **16 条分液记录**。
- 所有记录溶剂编码均为 `CGRJ-001`；枪头模块均为 `QT-001`，枪头 X/Y 均为 `(0,0)`。
- 导出的 16 条记录均为每孔 **1000 µL**。截图顶部虽显示 50 µL，但不能据此认定保存后的逐孔数据也是 50；用户已明确新建实验任务改为每孔 50 µL。
- 截图的两条样品瓶条码输入框为空；本轮没有编造实际样品条码，也没有创建样品身份。

## 已确定的大瓶—目标位置关系

两个来源位置的 X/Y 都为 `(1,1)`，必须通过来源模块编码区分，不能仅凭坐标或行顺序判断瓶号。

| 大瓶/条码槽 | 来源模块与位置 | 目标模块 | 目标 X/Y 范围 | 孔数 |
| --- | --- | --- | --- | --- |
| 1 号 / SampleBarcode1 | CYC-001-1000，(1,1) | FYB-001 | X=1、2；每列 Y=1、2、3、4 | 8 |
| 2 号 / SampleBarcode2 | CYC-002-1000，(1,1) | FYB-002 | X=1、2；每列 Y=1、2、3、4 | 8 |

每个目标模块的导出顺序均为：`(1,1), (1,2), (1,3), (1,4), (2,1), (2,2), (2,3), (2,4)`；先 1 号瓶的 8 条，再 2 号瓶的 8 条。直接保留模板的模块和数值坐标，不另推断字母孔号。

## 实际实验时的新任务要求

1. 为新验收任务选择来源模板 `test`；不要误沿用指向 `TEST-001` 的旧配置或修改已发布工作流定义。
2. 保留上述 2 个来源、2 个目标模块和全部 16 个孔位及顺序，将**每条分液记录**的 `VolumeMicroliters` 设为 **50**，而非只修改页面顶部显示值。
3. 生成新的唯一厂家任务号。原模板/原始 1000 µL 数据与新生成的 50 µL 数据分开保存，保持来源证据；不得覆盖原 `test`。
4. 本轮移液计划量：每只大瓶 `8 × 50 = 400 µL`；合计 `16 × 50 = 800 µL（0.8 mL）`。这只是目标分液量，不包含预吸、残液或其他厂家动作的耗液量。
5. 创建时绑定本轮已核对的两条样品来源 ID/条码；没有现成条码时可按既定方案由中控生成来源 ID。模块编码不是样品条码，不能直接替代样品身份。
6. 保存新准备记录后，显式导入并回读：核对新任务号、16 条记录、来源/目标位置、每孔 50 µL 及双条码更新确认。导入不启动；通过后再按现场安排进行运行准入。

## 创建前需要处理的兼容点

- **枪头 `(0,0)`：**初次核对时旧解析规则要求全部坐标为正整数，因而拒绝模板。用户随后确认枪头由工作站默认管理；现已只兼容完整 `(0,0)` 并原样保留，不改成 `(1,1)`，不增加枪头选择/管理功能，来源和目标坐标仍必须为正整数。该本地兼容不等于新版厂家程序已真机验收。
- **本机运行版本：**2026-09-20 只读核对时，15041 上的旧适配服务可读取原始协议模板，但请求强类型 `/tasks/TEST-001/template` 返回 404；后续需核对/启用带新模板接口的最新构建，开盖分液专用 MES/WPF 也需核对启动版本。不能改动其他联调实例，更不能把旧任务号启动测试当作新模板导入验收。

本轮只整理文档和创建约定，没有导入、写条码、初始化、启动设备或合并日常分支。后续真实验收通过后，仍按既有条件授权合入 `feature/wpf-ui-layout-optimization`。
