# 开盖分液工作站 Beta 联调记录与待确认事项

后续更新：16:20 已收到带扫码选项和任务状态回写代码的新版；最终运行确认仍保留，用户选择“否”。当前进度以 [最新版联调记录](2026-09-14-workstation-latest-confirmation.md) 为准，下文保留 Beta 阶段的现场证据与当时待办。

记录日期：2026-09-14。状态：现场已确认进入真实执行；状态回传仍待厂家检查；人工确认已确定由厂家增加可选开关，待交付验证。

## 已验证的进展

- 厂家文件：`res/DLHWorkstation_Beta.exe`，1473024 字节，版本 1.0.0.0。
- SHA-256：`FAB2757664594CCABB7BD7FD40FA726E02965A50E5A5B2A91119BB5597E34C71`。
- 本地副本静态核对：已补上执行任务列表的 `Add(val)`；主界面远程任务分支已恢复定时器，并增加页面打开标志。文件核对不等同于现场进程哈希核对。
- 厂家 HTTP 服务：`http://192.168.200.157:8082/Service/`。
- 联调任务：`TEST-001`；源配置编码 `CYC-001-1000`，源 X/Y=1/1；枪头位置 `QT-001`，X/Y=1/1；目标 `FYB-001`，X/Y=1/1；分液量 50 μl。

### 本轮执行经过（北京时间）

1. 14:25:40 只读检查：设备 Idle、错误码 0、任务 Waiting，任务参数与此前确认一致。
2. 14:26:28.017 通过隔离分支 MES → Adapter → WCF 单次发送 `TEST-001` 启动；2.080 秒后收到 `Code=200, Data="启动成功"`，MES 返回 `acknowledged=true`。
3. 现场出现材料准备确认框。确认后提示需要录入 1#样品位样本条码，此次启动流程因条码未录入退出。
4. 用户在当前实验页面录入样本条码。考虑到再次远程启动可能重建页面并清空页面内的条码变量，未重发 HTTP 启动；由用户在当前页面手动点击“开始运行”并完成现场操作。页面重建导致条码丢失属于代码层面的可能性，本轮没有重发去验证。
5. 约 14:36，用户明确反馈“现在在执行”，截图显示“开始运行”禁用、“暂停/停止”可用，孔位颜色发生变化。现场真实执行已确认。
6. 截至本记录，尚未收到本次任务完成或故障的最终现场结论，不记录为任务已完成，也不记录为全自动闭环验收通过。

本轮 Beta 的 Adapter 日志中，实际发送 `StartExperiment` 仅 1 次；之后均为只读查询。没有另外发送初始化、避让、停止、删除或任务创建命令。

## 待厂家回复：状态回传不一致

14:36:35，经 MES/Adapter 读取仍为设备 Idle、任务 Waiting、错误码 0。14:36:59，绕过 MES/Adapter，带禁止缓存头直接读取厂家 WCF，仍得到：

```text
GetInstrumentStatus?EquipmentNo=TEST-001 → {"Code":200,"Data":"0"}
GetTaskState?TaskNo=TEST-001              → {"Code":200,"Data":"等待运行"}
```

因此已确认现场执行与厂家返回状态不一致，不是仅由 MES/Adapter 状态映射造成。具体回写、数据库配置或同步原因由厂家继续检查。用户已联系厂家，等待回复。

目前不能仅凭这组接口的 Idle/Waiting 判定现场可以再次启动；错误码 0 也不证明当前任务完成。任务详情的三个时间字段为 null；旧 WCF 详情实现没有填充这些字段，不能将其单独当作未运行的证据。

厂家反馈后，再验证同一任务的运行、完成及必要异常状态能否正确回传，暂不修改中控状态映射去掩盖不一致。

## 历史方案：厂家提供人工确认选项

此为 Beta 阶段的约定，已被后续决策替代：厂家取消远程运行二次确认窗口，中控在发令前确认，取消则不发送请求。以 [最新版记录中的最新决策](2026-09-14-workstation-latest-confirmation.md) 为准。

用户最初要求暂缓处理，倾向以后全自动运行；随后明确：由厂家在程序中增加一个勾选选项，通过选项状态决定是否需要人工确认。

- 当前行为：厂家 Beta 有材料准备确认、样本条码录入和实验运行确认等人工步骤。
- 目标行为：厂家程序支持需要人工确认和不需要人工确认两种模式，由现场选项决定；具体选项名称、默认值、勾选含义及覆盖哪些确认窗口，以厂家交付为准。
- 决策状态：责任边界及可选开关方案已确定，尚未收到带此选项的新版本，也未验证其行为。状态更新接口厂家仍在检查，与确认窗口开关分别跟踪。
- 当前动作：不修改厂家 EXE/DLL、不修改我们的启动逻辑、不增加代点弹窗或自动确认措施。
- 新版交付后：分别验证人工确认开启/关闭时的远程启动表现，并检查状态回传。样本条码是独立的数据输入需求，不能因关闭确认弹窗就假定条码已自动提供；其自动化方式仍需核实，当前不另行实现。

## 工作区与复测衔接

- 本轮使用隔离分支 `feature/sample-workstation-http-readonly` 的 `29343b8` 代码，没有修改中控主工作区、现场程序或数据库。
- 本机临时测试入口：MES `127.0.0.1:15045`、Adapter `127.0.0.1:15041`；独立临时数据库，真实工作站 HTTP、模拟 AGV，AUBO 和自动物理工作流关闭。记录时本机测试进程仍保留，未追加设备操作。
- 日志目录：`C:/Users/33206/AppData/Local/Temp/mes-workstation-beta-20260914-142603/`。
- `EquipmentNo=TEST-001` 沿用联调占位值，不代表已确认厂家真实设备编号。
- 后续等待厂家状态接口检查结果及带人工确认选项的新版本，再按实际现场状态安排下一次验证；不自动重发已有任务。

历史记录：[最早远程入口缺失](2026-09-14-workstation-remote-start.md)、[new 版任务列表缺漏](2026-09-14-workstation-new-start-ack.md)。中控模块入口见 [接入说明](../SAMPLE-WORKSTATION-CENTRAL-INTEGRATION.md)。
