# 全景摆正、俯视及工作台轮廓示意对齐

## 最新用户授权

用户先批准摆正相机与独立俯视；实施中补充箭头截图，并确认“对，把这两处对齐”：黑色导航地图工作站轮廓对应白色开盖／分液工作台。此确认替代旧 LM1／LM6 红框粗估变换的优先级。

AGV 已由用户手动接管去充电，本阶段不发送任何控制指令，也不续跑两轮测试。上次只完成 LM1→LM7、LM7→LM6 两段。运行服务保持原样，不提交、不合并。

## 显示变更

- 全景相机沿 CAD 轴摆正，45° 俯视；新增工具栏“正上方俯视”，使用正交投影，无需进入标定模式。
- 俯视支持右键平移、滚轮缩放、设备点击与聚焦；切回“设备区／完整场景”恢复摆正的透视视角。
- 窗口 resize 和首次位姿 snap 保留相机类型／朝向；显式视角切换不改世界坐标，不暂停示意跟随。
- 构图包括站点和曲线拐弯的运行范围。标定选点期间设备标签不截获点击，避免遮挡地面拾取。

## 新示意参照（不是测量标定）

地图工作台轮廓约 x=1.9..4.64、y=-2.32..-0.84，选中心 (3.27,-1.58)；CAD 对应岛台区域约 x=17.03..19.33、z=-7.788..-6.288，中心取 (18.18,-7.04)。

采用 theta=π、tx=21.45、tz=-5.46、地面 Y=-.025：mapX 正向对应 CAD -X，mapY 正向对应 CAD +Z。主要边缘摆正，轮廓中心重合；形状存在差异，不缩放地图或设备以强行贴边。

新的示意站点 LM1=(21.45,-4.66)，LM6=(17.779,-4.375)，间距仍为原地图约 3.682 m。站点、路线、AGV 位置／航向共用这一变换。不写正式 calibration.json，界面明确工作台轮廓对齐、非测量标定。

车头检查：拆分脚本仅减去局部原点，没有额外旋转；航向公式与坐标轴约定一致。但资产没有可信车头标记，不能从几何长轴判断正反。保留车头修正 0，不宣称现场朝向已验收。

## 验证记录

- JS 相机／地图／插值 11 项通过。
- WPF 回归 533 项通过；独立相机审查及工作台对齐增量审查无待修复问题。
- 原生离线冒烟 `artifacts/digital-twin-view-alignment/smoke-workstation/result.json` 通过，包括俯视连续坐标更新、首帧／resize 保持、6 站点构图、实际射线选设备并聚焦、标定地面拾取、低置信度／过期冻结、退出卸载。
- 截图同目录 `aligned-overview-lm1.png`、`aligned-top-lm1.png` 为离线样例，不是现场行驶证明。
- 初次冒烟遇到测试表达式括号遗漏，已修正；另一次测试在切换合成标定时未等待新源帧，触发原有跳变守卫，已修正测试等待逻辑；俯视中心标签截获标定点击的问题已修复并复测。
- GLB SHA256 保持 `96ca7f34c189c9c1add62c29441a7a9b16e67619c96659c259dca7ea8412671a`，1,287,310 三角面不变。

新包路径：`artifacts/digital-twin-view-alignment/package`。旧包保留。现场路线／车头最终确认需用户恢复实机测试后观察，当前不启动车辆。

## 现场预览与用户反馈

已确认旧预览没有标定选点草稿后正常关闭 PID 23968，启动新版只读预览 PID 27124。启动证据 `artifacts/digital-twin-view-alignment/live-session/startup.json`：sceneReady=true、schematicFollowing=true、poseValidation=null，正常读取 LM1 静止坐标；没有设备控制或新调度器。MES/Adapter 原有监听 PID 保持不变。

用户查看新版后反馈“目前最新的这个位置图差不读”（按上下文理解为“差不多”）。当前版本作为视觉示意基线保留，不继续调参。此反馈仅代表静态位置关系基本认可，不代表往返路线、停车点精度或车头方向已完成实机验收。AGV 控制仍暂停，须用户重新确认后再进行现场运动测试。
