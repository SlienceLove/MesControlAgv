# 数字孪生全屏版：已切换，用户验收通过

## 2026-09-20 现场显示验收

用户确认切换后，已打开全屏版主中控PID22344，原本轮主中控PID29900正常关闭；更早的旧窗口19632保持原状。MES30812和Adapter39856均未重启，流程库哈希未改变，主WPF已建立连接仅到本机MES15445，没有设备控制操作。

已现场验证F11进入纯三维全屏、Esc恢复普通布局，再次进入后停留全屏供查看。截图确认详细状态卡/说明隐藏，AGV与工作站简短状态标签保留，真实坐标持续接入。证据：`artifacts/digital-twin-fullscreen/cutover-20260920/verification.json`及三张现场截图。

用户反馈“效果没啥问题”，本版全屏显示验收通过，作为当前可用版本保留；旧包继续用于回退。本次为显示与静态真实数据验收，没有新增移动验收，不改变设备动作授权。

## 实现结果

用户确认纯三维全屏范围后，在原隔离工作区实施，未提交、未合并。

- 数字孪生页新增“全屏查看 · F11”；按钮/F11进入，退出按钮/Esc返回。
- 同一主窗口无边框全屏：隐藏导航、中控抬头、三维长说明/标定参数、底部三张详细状态卡和时间戳等文字。正常模式退出后全部恢复。
- 全屏只留全景/设备区/地图参照/退出工具栏，保留模型与简短状态标签。位置断线、过期、未启用转换、暂停和跳变仍有简短警示。
- 原WebView、模型、状态与位姿读取会话不重建；没有新增轮询、服务或设备控制调用。地图开关、示意跟随选择和模型世界坐标保持。
- 退出恢复原窗口位置尺寸、样式和最大化状态。转入其他中控页自动退出全屏。

## 验证

证据根目录：`artifacts/digital-twin-fullscreen/`。

- WPF 546/546：`tests/wpf-fullscreen.trx`。
- JS 动画/地图/相机20/20。
- 同树/同会话、详情隐藏、简短标签、定位错误恢复、模型坐标、地图选择、正常/最大化恢复、按钮和键盘桥：`smoke-r1/result.json`。
- Windows键盘实际送入已聚焦离线WebView：`smoke-native-keys/result.json`，`nativeKeyboard=true`。输入前核对测试窗口前台、document.hasFocus及无修饰键，仅允许Esc/F11，未向运行中的现场中控输入。
- 原位姿平滑/标定/工作站无响应/卸载验证：`smoke-pose/result.json`。
- 主窗口MES HTTP替身、404/501/504/身份/地图/过期及切页：`smoke-main/result.json`。

以上均为离线HTTP替身，没有控制现场设备。截图 `smoke-native-keys/fullscreen-offline.png` 中工作站“读取失败”是故意设置的离线条件，不代表现场工作站异常。

代码审查未发现Critical/Important，指出原CDP/WPF事件注入不等同真实焦点键盘路径、自动重复测试注释不准确；已修正说明并补齐repeat事件和Windows焦点键盘测试。

## 发布与运行边界

- 新WPF：`artifacts/digital-twin-fullscreen/wpf/`。
- 新版入口：`artifacts/digital-twin-fullscreen/Start-606CentralClient.ps1`（`-InspectOnly`仅检查，不启动）。沿用已有外部MES15445/Adapter15441、流程库和地图；仍不自动托管服务、不绑定历史流程。
- WPF DLL SHA256：`A92C201A0DF14D33079CA97881C20C7C8F63646A7CA818406EB2CE466785139C`。
- GLB SHA256未变：`96CA7F34C189C9C1ADD62C29441A7A9B16E67619C96659C259DCA7EA8412671A`。
- 打包时主中控29900尚未替换；随后已受控切换为22344，详见本文件现场显示验收。网关39856和MES30812未重启，旧中控19632继续保留，独立预览11040已在上阶段关闭。

运行切换和屏幕验收现已完成。后续使用本目录启动入口；任何新设备动作/移动验证均单独确认。
