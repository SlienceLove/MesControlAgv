# A阶段实施清单

1. 使用既有隔离worktree，运行准备ViewModel/排程绑定基线。
2. 新增统一体积命令、计划汇总与未保存提示，复用现有编辑回调；不修改后端契约。
3. 更新现有XAML并增加离线ViewModel与布局绑定测试。
4. 跑WPF相关回归和隔离Release构建；独立审查，记录证据并提交。

writing-plans技能文件不可用，沿用仓库实施清单fallback。用户已确认设计范围，不再重复请求同一范围审批。
