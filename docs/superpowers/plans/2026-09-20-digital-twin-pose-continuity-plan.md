# 动画衔接实施清单

范围以同日 pose-continuity-design 为准。用户已批准持续推进至需要实机验证。使用既有隔离工作区，不提交合并，不导航，不改变运行 MES/Adapter。

1. WPF/JS 基线；在 pose-motion.mjs 实现可传入时间的自适应动画状态机。
2. 加入均匀/抖动帧、续接、静止重复帧、最短角差、有效期、停止/首次定位测试；用固定输入计算旧版与新版等待占比。
3. viewer 接入状态机，保持相机/地图/画质；增加轻量只读动画诊断供离线测量。
4. 扩展 WPF/WebView 离线测试，实际500ms采样下检查运动中间帧及停稳、冻结保护。运行全量相关回归与原生冒烟。
5. 独立代码审查，处理必要反馈，构建新预览包，旧包保留。写出量化结果和局限，在需实机复验时交接，不自行启动实机。

完成状态：以上5项均完成；JS19项、WPF533项、原生匀速与抖动转向及旧功能回归通过；独立审查反馈已修正。交付与量化证据见 docs/DIGITAL-TWIN-POSE-CONTINUITY-2026-09-20.md。当前停在需要用户现场实机流畅度复验的边界，没有再发任何设备命令。
