# 短任务号实施清单

1. 保持既有隔离工作区；记录基线，新增短号、固定时钟、多次准备、幂等和历史长号Unknown测试，先确认旧实现不通过短号断言。
2. 仅调整CreateVendorTaskNo及调用，使用WS+18位大写随机十六进制。
3. 离线还原本轮实际上传XLSX并验证原hash；整理厂家复现包，不替换现场文件。
4. 运行MES/Adapter回归和隔离Release构建；独立只读审查，修复实际发现；更新交接和验证记录后提交。

不发送厂家请求，不修改现场服务/数据库，不合并日常分支。writing-plans技能不可用，本清单为fallback。
