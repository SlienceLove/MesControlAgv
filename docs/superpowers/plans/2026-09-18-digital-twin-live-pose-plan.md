# AGV 三维位姿实施记录

用户已于 2026-09-18 确认设计。设计：../specs/2026-09-18-digital-twin-live-pose-design.md。

writing-plans 技能不可用，采用本清单逐项实施。现有独立工作区 feature/three-device-sequence，HEAD 201dbb4，不提交／合并、不部署到运行实例、不发送实机动作。

- 基线：隔离工作区 WPF 447/447 通过。
- 已选择性复制已有数字孪生源文件、离线 Web 资产与两组测试；运行 GLB SHA256 保持 96ca7f34c189c9c1add62c29441a7a9b16e67619c96659c259dca7ea8412671a。
- Task 1：完成。只读 1101 位姿＋1300/1302 地图身份 → Adapter → MES；协议、只读性、异常和身份测试通过，新代码实机只读取得 3 帧。
- Task 2：完成。页面生命周期采样、坐标状态、三维地面选点、刚体标定与保存、整机跟随；未启用猜测坐标。标定参数更改使人工确认失效。
- Task 3：完成软件验证与审查。WPF 529、Adapter 329、MES 529、相关 TCP E2E 3、JS 2 项通过；离线原生 WebView 验证通过。现场标定与运动一致性验收未完成，不作为软件测试通过结论的一部分。

审查记录：独立 reviewer review_pose 只读审查；旧接口 404/501 提示和数值参数变更后确认失效两项已修复，补测试并复核通过。顺带补齐新增 semaphore 释放。

测试基线差异：相关旧 E2E 的地图 API 顺序断言仍是 1300→1301→1302，但 HEAD 的既有实现已经是 1300→1302→1301（MD5 优先缓存验证）。只修正该断言，不改已有预检行为。新增端点 E2E 初次工厂配置缺少批准的 MinimumConfidence=0.98，已按原测试配置修正。
