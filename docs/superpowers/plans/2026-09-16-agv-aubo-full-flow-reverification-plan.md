# AGV + AUBO 单次完整流程重新验证计划

日期：2026-09-16。状态：已完成。名称比较修复、历史返航核销和控制权释放均已落实；新 WPF run `d5ebfe6d-1968-4323-8f0f-91c5c11ebcb4` 于10:54:54自动 Completed，7/7动作成功，自动释放控制权，最终相关回归324通过。详见[完整通过记录](../../physical-acceptance/2026-09-16-agv-aubo-full-flow-passed.md)。

以下为启动前计划及核对记录，实际结果以上述执行报告为准。

## 目标与依据

用户最新确认：“现在确认路由不会断联了，可以执行流程了。”据此进入真实流程验证阶段，不重复要求网络稳定性确认或重做长时间网络监听。网络稳定是用户现场确认，不把它写成本轮助手完成的测试。

沿用已批准的[命令恢复计划](2026-09-14-agv-command-reconnect-plan.md)与[最小联调等待方案](../specs/2026-09-14-commissioning-wait-simplification-design.md)。本计划补充现场步骤，不增加软件功能或重构。

成功目标：从实际 WPF 发起一个新 RunId，完成四段导航和三次 AUBO 回原点，WPF、MES 与设备回执一致，并保存结果记录。

## 本轮已核对

- HEAD 仍为 `0e785b4`；工作区有 75 项已跟踪变更、335 项未跟踪条目，均未改动或批量提交。
- Adapter 5141 / PID 41248、MES 5145 / PID 47356 仍监听。PID 仅为本次观测，实际执行前按进程身份核对。
- MES 实时查询显示旧 run `39fca078-edfc-4a0f-85c5-44348dcb69ab` 仍为 `runtimeStatus=4 (Paused)`，待执行“站点2：回原点.pro”。
- 原 WPF 流程文件中的流程 `465daad1-a127-4bb2-876d-c973415a1dc3` 包含下列七个动作；文件的 `PublishedVersion` 为 null，旧 run 使用服务端 v1。启动时需核对实际 WPF 加载文件与服务端定义，不能把本地未标版本当成服务端未发布。
- 未发现 WPF 进程。启动时定位既有可执行文件、参数和流程存储位置。
- 已读现有取消实现：仅在没有 Claimed/Running/Unknown 节点和 Accepted/Running/Unknown 设备操作时允许取消，保留已完成记录及控制审计。未调用取消接口。

## 执行顺序

### 1. 启动前核对一次

- [ ] 读取 AGV 当前站点、空闲状态及控制归属；读取 AUBO 自动模式、程序停止状态与急停/保护停状态。
- [ ] 核对三段均使用 `回原点.pro`，程序具有结束节点。只补充尚未明确的现场监护信息，不重复询问用户是否允许执行流程。
- [ ] 保持 AP 看门狗关闭；做必要的即时连通核对，不改网络配置、不重开持续监听。
- [ ] 核对运行实例继续使用原数据库和流程文件；打开正确的 WPF 实例，检查 MES 地址与本次流程定义。
- [ ] 保存旧 run、nodes、device-operations、field-navigation-acceptances 和 timeline 的当前只读快照。

### 2. 处置旧暂停流程

- [ ] 确认第二段机械臂未下发，已完成的三节点及设备操作无待核对状态。
- [ ] 满足现有条件后，用新的 requestId、当前操作者及原因，通过现有 `/api/workflow-runs/{id}/cancel` 入口关闭旧 run；回读终态与审计。
- [ ] 若接口发现活动或结果不明的任务，先按已有恢复入口核对该任务；不清库、不手改状态、不放宽取消条件。

旧流程的 3/7 完成记录保留，不计入新一轮成功数量。

### 3. 确定起点并从 WPF 启动

- [ ] 若 AGV 当前位于 LM1 且空闲，直接准备新流程。
- [ ] 若不在 LM1，先报告实际起点与目标 LM1，按交接要求取得这一次独立返航确认后单次执行；使用全新命令 ID。不得直接重跑历史 `return-once.ps1`。
- [ ] 从实际 WPF 使用当前设备状态建立本次授权并发起单次真实流程，记录新 RunId、requestId、关联 ID、版本与启动时间。

| 动作 | 设备与目标 | 完成证据 |
| --- | --- | --- |
| 1 | AGV：LM1 → LM7 | 匹配本次导航任务的到站回执 |
| 2 | AUBO：`回原点.pro` | 匹配本次操作的程序完成回执 |
| 3 | AGV：LM7 → LM2 | 匹配本次导航任务的到站回执 |
| 4 | AUBO：`回原点.pro` | 匹配本次操作的程序完成回执 |
| 5 | AGV：LM2 → LM7 | 匹配本次导航任务的到站回执 |
| 6 | AUBO：`回原点.pro` | 匹配本次操作的程序完成回执 |
| 7 | AGV：LM7 → LM1 | 到站回执及现有流程要求的控制权释放结果 |

七个动作不包含画布上的开始、结束节点。三次回原点仅验证程序执行链路，不代表真实取料、分液或分析动作已经验收。

### 4. 观察、异常处置与收尾

- [ ] 观察到站后自动推进机械臂、机械臂结束后自动推进导航，核对 WPF 当前节点与 MES 投影一致。
- [ ] 正常 Waiting/Running/到站过渡继续等待；短暂读取失败只重试读取。保留已有时限，不重复加载地图或整套运行期 Ready/授权检查。
- [ ] 明确故障按现有逻辑处置；命令结果不明则暂停后续派发、保留证据并走人工核对，不重发控制命令。软件流程暂停不等同物理急停。
- [ ] 最终核对新 run 成功、7/7 动作成功、AGV 位于 LM1、AUBO 程序停止、无活动或结果不明的设备操作。
- [ ] 保存本次 run/nodes/device-operations/导航回执/timeline、WPF 结果画面及结论到独立的 `artifacts/physical-acceptance/` 会话目录。保存实际执行时间与 ID，不预填成功结果。

## 通过条件与后续优先级

本轮通过必须同时满足：七动作真实完成；各动作只派发一次；状态自动推进与结果记录一致；无未处理 bug。网络稳定或三个旧节点完成不能替代这些条件。

如果现场暴露 bug，只修复有证据的阻塞点，运行对应回归并核对部署版本后重新做完整单次验证。若无代码变更，不为规划重跑全仓测试；形成验证版本提交前仍核对相关测试记录与待提交 diff。

通过后依次推进：

1. 整理本次现场报告，按已有要求评估并提交范围明确的“现场验证版本”，保留无关改动。
2. 厂家模块到位后分别接入开盖分液、离子色谱的真实动作与结果回传，再纳入完整业务流程；未到位则记录阻塞，不标记已接通。
3. 在设备执行链路稳定后验证最小样品记录、扫码、Excel 人工导入及结果关联；UI 优化继续靠后。

本计划未执行部署、返航、流程取消或机械臂命令，也未提交验证版本。
