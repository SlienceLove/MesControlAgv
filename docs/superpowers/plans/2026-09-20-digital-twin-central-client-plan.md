# 数字孪生日常中控接入实施清单

设计已获用户确认；在 three-device-sequence 独立工作区实施，不提交、不合并。

- [x] 核对既有 WPF→MES→Adapter 位姿链路与独立工作区；Adapter 329、WPF 533 基线通过。
- [x] 增加显式 WPF_TWIN_SCHEMATIC_FOLLOW 启动配置；仅物理模式且配置有效时允许，默认关闭，主窗口仅应用一次。
- [x] 增加外部服务日常启动脚本：MES15445/Adapter15441，不托管服务，不自动绑定流程或发动作。
- [x] 启动配置测试、主窗口原生离线烟测（MES HTTP 替身、切页、取消、错误与恢复）、GET 位姿链路回归。
- [x] 完整 Adapter334/MES535/WPF543/JS20 回归和既有原生位姿烟测。
- [x] 新目录发布 WPF/Adapter，校验模型与配置，独立只读代码审查；修复历史流程环境变量继承并添加回归。
- [x] 就绪交接文档 DIGITAL-TWIN-CENTRAL-CLIENT-2026-09-20.md；运行切换留待下一次明确确认，本阶段不移动车辆。

验收保留：整体 AGV+机械臂、既有示意变换和平滑动画、地图参照默认关、无俯视入口、D160/SHA18i 未绑定、正式标定文件不变。
