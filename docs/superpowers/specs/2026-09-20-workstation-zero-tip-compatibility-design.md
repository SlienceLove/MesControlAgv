# 枪头零坐标最小兼容与本地 50 µL 预览

用户已确认：只兼容完整枪头 `(0,0)` 并原样保留，其他孔位规则不变；生成两来源、16 孔、每孔 50 µL 新模板，仅做本地验证，不向真机导入或启动。

用户随后明确：枪头按工作站默认逻辑，中控不管理使用哪个枪头。因此不新增枪头选择、分配、库存或核对功能；中控仅原样传递模板默认值。厂家沟通重点为逐目标体积与双来源导入保持。

## 依据和限制

- `test` 实测 Excel 及 `TrajectoryParameterDetails?key=test` 均返回 `QT-001`、`WarehouseX=0`、`WarehouseY=0`。不是仅 Excel 导出缺值。
- 厂家 V1.02 PDF 第 21 页只定义整数枪头坐标，没有规定零值含义。
- 既有 `LidRotatePipettingPlatformForm.dll`（2026-09-09，SHA256 `93B75B12F29D089D1E177961D44CAA285C2B0938AE93C407AA68D20C87C1DACE`）中，`OnePumpTakeNeedleMovement.AactualTask` 调用 `UseNeedleParamStatus.GetCanNeedle` 获取可用枪头，再用所选位置计算运动坐标。旧实现说明工作站负责选枪头，但不能证明当前现场新版实现完全相同。
- 本次将 `(0,0)` 作为厂家实际导出的合法表示原样往返，不把它改成 `(1,1)`，不宣称已确认现场自动取枪头模式。

## 最小改动

1. 仅调整 `SampleWorkstationTemplateFile` 的枪头坐标读写：允许双正整数，或完整 `(0,0)`；拒绝半零、负数、非整数和缺值。来源/目标坐标、移液量仍为正整数。
2. 不改 Contracts、调度、运行准入、worker、UI 或现场配置。导入逐项回读比较照旧，不能认为 `(0,0)` 等于 `(1,1)`。
3. 固化 `test` 的只读模板响应作为本地 fixture，测试实际 16 行。生成预览时使用新任务号及明确的本地预览名称 `LOCAL-PREVIEW-50uL`，每行体积改为 50；其余转移字段、顺序、两个来源各 8 个目标和枪头零值保持不变。
4. 提供小型离线预览工具：从已保存的模板响应生成独立 XLSX，使用新任务号，不覆盖现有文件或原 `test`。无 HTTP 客户端、MES 样品核对或执行调用；预览文件不是已导入/可直接准入的凭据。

## 验证与交接

覆盖真实零坐标 fixture、读取/写入往返、半零/负数/其他孔位零值拒绝、读取成功但枪头回读变更仍拒绝、两来源16孔50µL预览。运行 Adapter/MES 相关回归与隔离构建；独立审查后提交。

厂家待确认：任务顶部体积与逐目标保存值的关系；两来源任务导入后是否保持各自目标关系。枪头由工作站默认管理，中控原样传递模板值，不要求用户另行研究枪头选择。现场导入/运行仍留待后续验收，不改原 `test`，不合并日常分支。
