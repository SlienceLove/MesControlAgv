# 导航地图参照与轻量地面

用户已确认推荐方案：浅灰地面、深色轮廓、1 米网格、LM 站点和可开关路线；不建完整墙体。用户提供的截图 383545a6-591f-4a90-80a6-9e9599bba6ff.png 在实验台区域右侧圈出 LM1 大致位置，不是精确三维坐标。

## 实现范围

- 原地图 guangzhou606.smap，MD5 9bd67a8b01f4da2617ce67e5f8a8d6b1。提取站点、特征线、扫描点和路径曲线；数据保留米制坐标，独立静态 JSON 打包，原 SMAP/GLB 不修改。
- 默认工作区底板按站点包围盒加 3 m 留白取尺寸，至少覆盖现有 CAD 模型；浅灰地面、深色工作区边框、1 m 网格。矩形边框只是显示范围，不声称为房间外墙；地图扫描范围也不等于房间形状。
- 标定前无法唯一确定地图与 CAD 的旋转和平移。三维先显示中性的网格底板，地图轮廓、LM1/LM2/LM6/LM7（兼含 LM4/LM5）及路线在独立导航参照小窗显示，明确“未与 CAD 对齐”。不采用任意旋转平移后再诱导用户以该站点图标做标定。
- 用户确认有效标定后，以已有刚体变换将相同地图数据投影到三维地面，站点和轮廓按地图正确相对关系叠加；仍不是新建实体墙。重新进入标定时隐藏三维地图叠加，避免将底图站点当作独立现场观测。
- 提供地面、地图参照、路线开关以及地面全景，保留设备聚焦和原有光照、模型清晰度。初始镜头优先设备区与附近地面，避免为完整扫描边界把模型缩得太小。
- 在 JSON 验证、坐标投影/范围、线路开关、校准前后叠加、资源释放方面测试；模型仍为 1,287,310 面。额外平面和线段开销单独记录。
- 复用独立 CalibrationPreview 发布新包，不改现场 MES/Adapter、不自动保存标定、不发起实机移动。更新预览前只关闭本任务旧预览窗口；不关闭现场中控。

## 实施清单

1. 提取并校验只读地图资产。
2. 地面/网格、导航参照小窗、路线开关和经确认的三维叠加。
3. 原生 WPF 冒烟、模型资产哈希、独立审查。
4. 修复此前预览启动截图与关窗时序问题，发布独立新预览供用户继续标点。

writing-plans 技能不可用，以上清单作为实施计划；不自动提交或合并。
