# 2026-09-14 AMR 周期性断线诊断归档

状态：本次网络诊断收尾；用户要求停止监听，真实流程留待后续重新验证。
时间均为北京时间（UTC+08:00），AP 截图自身时钟除外。
新会话先读仓库根目录 [NEXT-SESSION.md](../../NEXT-SESSION.md)。

## 结论与边界

现有证据强烈支持：AP 上以 AGV 为目标的 Ping 看门狗配置，是本次周期性无线断线的主因。
用户已关闭看门狗；关闭后固定周期断线没有复现。建议保持关闭，不同时调整无证据指向的网卡驱动、电源或其他无线参数。

AP 看门狗探测失败触发恢复/重启的解释与记录高度吻合，但未获得断线后的第二份 AP 启动日志或固件内部触发记录。
此前 AGV 开机、电脑能访问它时，AP 自身为什么仍探测失败，尚未独立证实。
不把这个结论写成“所有历史断线原因均已排除”，也不把网络观察写成“真实全流程验收通过”。

## 关键设备与配置

| 项目 | 已记录值 |
| --- | --- |
| AP | TP-LINK TL-XAP1506GC-PoE/DC 易展版 V2.0 |
| 固件 | 1.0.14 Build 20250428 Rel.55168 |
| AP 管理地址 / BSSID | 192.168.1.254 / 9C:47:82:D5:06:3F |
| 控制无线 | AMR，2.4 GHz；本机 WLAN 3 / COMFAST USB 网卡 |
| 本机 / AGV / AUBO | 192.168.1.11 / 192.168.1.2 / 192.168.1.102 |
| 原看门狗设置 | 开启；目标 192.168.1.2；周期 300 秒；启动延迟 300 秒；最大丢包 3 |
| 本次唯一网络配置调整 | 用户关闭 Ping 看门狗；助手未登录管理接口或修改其他配置 |

AGV 约 15:09–15:10 被用户断电充电，外接 AP 持续供电；之后周期性断线仍继续。

## 证据链

1. 当日早期统计共 53 次断开；51 个规律相邻间隔平均 956.3678 秒，范围 956.192–956.635 秒。现象跨软件重启和无动作时段出现。
2. AP 截图含 `System Start`，正好 300 秒后出现 `PWDOG: Ping Watchdog test start!`。AP 日期为 2025-04-28，不与电脑日期直接对时。
3. 授权 ETW 在 16:08:03 捕获目标 AP 的 `DisAssoc ... Reason: 0x10003`；微软 SDK 定义对应收到对端解除认证，低 16 位原因码为 3。它不是单纯应用 TCP 超时，也不能单凭此码证明固件内部重启原因。
4. 16:08:13 本机记录 AMR 媒体断开；16:09:18 重连。采集于 16:09:20 自动停止。
5. 用户关闭看门狗后，自 16:21:27 至 16:49:01 观察约 **27 分 34 秒**，AMR 新增断线 **0**、重连 **0**；约 16:24:10、16:40:07 两个旧周期预测点均未复现。
6. 主动 Ping AP：**656 发 / 656 收 / 0 丢**，最短/平均/最长 **1/2/18 毫秒**。16:35:14 停止持续 Ping 后，被动观察约 **13 分 47 秒**，仍无断线。

原计划 35 分钟，用户提前结束，因此不能记作完整 35 分钟验证。主动 Ping 只覆盖其中一段，不能把 656 次响应写成整个观察窗口的连续探测。
最终累计事件查询覆盖完整实际窗口，WLAN 日志开启且查询无错误；保留的是检查点、部分 Ping 输出和最终统计，并非每条实时进度消息。

## 归档清单

证据目录：[network-investigation-20260914-1507](../../artifacts/physical-acceptance/network-investigation-20260914-1507/README.md)。

- `watchdog-off-final.json`：最终观察结果、Ping 统计、结束原因及边界。
- `amr-wlan-events.json`、`disconnect-intervals.csv`：变更前无线事件与周期统计（早期快照，不冒充最终状态）。
- `capture-20260914-155305/amr-wireless.etl`、`started.json`、`stopped.json`：授权底层采集及收尾记录。
- `screenshots/ap-system-log-before-change.png`：AP 系统日志截图。
- `screenshots/ping-watchdog-enabled-agv-target.png`：原看门狗配置截图。
- `screenshots/ping-watchdog-disabled.png`：用户关闭后的页面截图；未由助手进行认证接口回读。
- `evidence-manifest.json`：14 份选定静态证据的文件大小及 SHA-256；三张截图已从临时目录原样复制并核对源文件哈希。
- `run.json`、`nodes.json`、`device-operations.json`、`field-navigation-acceptances.json`、`timeline.json`：流程暂停阶段的投影快照。

未归档密码、带密码的设备标签照片；没有复制/覆盖正在使用的业务数据库。

## 真实流程留待下次

- 当前 run：`39fca078-edfc-4a0f-85c5-44348dcb69ab`，最终确认 `runtimeStatus=4 (Paused)`。
- 本次已完成 LM7 到站、第一段机械臂回原点、LM2 到站；7 节点流程仅完成 3 节点，第二段机械臂尚未下发。
- 用户后来给 AGV 断电充电，不能把最后业务到站 LM2 当成下次启动时的真实位置。
- 监听已停止；本次诊断/归档没有恢复流程、派发命令、执行机械臂程序或提交“现场验证通过”版本。
- 用户明确要求真实流程后续重新验证。下次从现场状态核对和用户当次确认接续，不直接恢复这个旧 run。
